{
  "name": "customeriq-web",
  "version": "1.0.0",
  "description": "CustomerIQ web dashboard consuming the FastAPI backend.",
  "private": true,
  "scripts": {
    "start": "python -m uvicorn backend.app.main:app --reload",
    "test": "python -m pytest backend/tests",
    "frontend": "python -m http.server 8080 --directory frontend"
  }
}
